# Core Dependencies
tensorflow>=2.15.0,<3.0.0
torch>=2.0.0  # PyTorch for hybrid ML engine
numpy>=1.24.0,<2.0.0
pandas>=2.0.0
scikit-learn>=1.3.0
pyyaml>=6.0.0  # For Ansible playbook generation
psutil>=5.9.0  # For system resource monitoring
tabulate>=0.9.0  # For markdown table generation

# Excel Support
openpyxl>=3.1.0
pyarrow>=14.0.0  # For Parquet file support

# Web Scraping & HTTP
duckduckgo-search>=4.0.0
requests>=2.31.0
urllib3>=2.0.0
beautifulsoup4>=4.12.0
selenium>=4.15.0
lxml>=4.9.0

# SOAP & Security
spyne>=2.14.0
lxml>=4.9.0
zeep>=4.2.1
docker>=6.1.0
certbot>=2.7.0
ansible>=8.0.0

# Prometheus Monitoring & Metrics Export
prometheus-client>=0.19.0

# ============================================
# LOG & MONITORING PLATFORM INTEGRATIONS
# ============================================

# AWS CloudWatch
boto3>=1.28.0
botocore>=1.31.0

# Azure Monitor
azure-monitor-query>=1.2.0
azure-identity>=1.15.0

# Elasticsearch/Kibana/Logstash (Elastic Stack)
elasticsearch>=8.0.0

# DataDog (APM & Monitoring)
datadog-api-client>=2.0.0

# Dynatrace (APM)
dynatrace-api-client>=1.0.0

# Splunk
splunk-sdk>=1.7.0

# SolarWinds (LEM/SIEM) - uses standard requests

# Grafana/Loki/Prometheus - uses standard requests

# Sumo Logic - uses standard requests

# Papertrail - uses standard requests

# Loggly - uses standard requests

# Fluentd - optional Elasticsearch backend
# Uses elasticsearch>=8.0.0 (already listed above)

# Retrace (Stackify) - uses standard requests

# Sentry (Error Tracking)
sentry-sdk>=1.40.0

# Coralogix - uses standard requests

# Lumigo (Serverless Monitoring)
lumigo-tracer>=1.1.0

# Huntress (Endpoint Security)
# Uses standard requests

# GoAccess (Web Log Analyzer)
# Requires goaccess binary installed on system

# Logentries (Rapid7 InsightOps) - uses standard requests

# VelocityAP (APM) - uses standard requests

# Biometrics Ltd DataLOG - uses standard requests

# Data Log Viewer - uses standard requests

# ============================================
# POSTGRESQL VECTOR DATABASE SUPPORT
# ============================================

# PostgreSQL adapter with connection pooling
psycopg2-binary>=2.9.9

# pgvector extension for vector similarity search
# Note: Install pgvector extension in PostgreSQL:
# CREATE EXTENSION vector;

# ============================================
# MACHINE LEARNING ENHANCEMENTS
# ============================================

# Already included above:
# - numpy>=1.24.0,<2.0.0
# - scikit-learn>=1.3.0
# - pandas>=2.0.0

# Additional ML dependencies for KNN and vector operations
# (Already satisfied by above requirements)

# ============================================
# OPTIONAL: Performance Profiling & Testing
# ============================================

# Memory profiling
memory-profiler>=0.61.0

# CPU profiling
py-spy>=0.3.14

# Load testing
locust>=2.15.0

# Development Dependencies
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.21.0
pytest-mock>=3.12.0

# Code Quality
black>=23.0.0
flake8>=6.0.0
mypy>=1.5.0
pylint>=3.0.0

# Build Tools
pyinstaller>=6.0.0
setuptools>=68.0.0
wheel>=0.41.0

# Type Stubs
types-requests>=2.31.0
types-psutil>=5.9.0
